<% title('Photos') %>

<% breadcrumb [["chotos","Photos"]] %>

<div class="container-fluid">
  <div class="row">

    <div class="col-lg-8">
      <% @charges.each do |charge| %>
        <div class="card">
          <div class="card-header">
            <a href="<%= charge_photos_path(charge)%>"><h5 style="display: inline-block;"><%= charge.long_name %></h5></a>
          </div>
          <div class="card-block">

          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
