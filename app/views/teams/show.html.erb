<div class="col-md-8">
  <div class="card">
    <div class="card-header">
      <h3><%= @team.long_name %></h3>
    </div>
    <div class="card-block">
      <p>
        <b><%= @team.long_name %></b> have entered <b><%= @entries.count %></b> charge<%= @entries.count>1 ? 's' : '' %> since
        <b><%= @entries.first.charge.ref %></b>
        raising <b>$<%= number_with_precision(@team.raised_dollars, precision: 0, delimiter: ',') %></b>.
      </p>
      <p>
        The team's best leg was <%= render 'entry_legs/description', entry_leg: @best_leg %>
      </p>
    </div>
  </div>

  <% @entries.each do |entry| %>
      <div class="card">
        <div class="card-header">
          <h5><%= entry.charge.name + ' ' + entry.charge.location %></h5>
          <%= entry.charge.entries.count %> entires.
        </div>
        <div class="card-block">
          <p>
            <b><%= format_position(entry.position_raised) %></b> place in fundraising. Raised
            <b>$<%= number_with_precision(entry.raised_dollars, precision: 0, delimiter: ',') %></b>.
          </p>
        </div>
        <div class="card-block">
          <% entry.result_summary.each do |res| %>
              <%= res %>
          <% end %>
        </div>
      </div>
  <% end %>
</div>

<div class="col-md-4">
  <div class="card">
    <div class="card-block">
      The money raised by <b><%= @team.long_name %></b> has supported conservation in Zambia through the following
      organisations and projects:
      <div class="card-block">
        <div class="row">
          <% @team.beneficiaries.each_with_index do |b, i| %>
              <div class="col-md-12 col-lg-6">
                <%= render 'beneficiaries/beneficiarythumb', beneficiary: b %>
              </div>
              <% if (i+1)%2==0%>
                </div>
                  <div class="row">
              <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
